# 講故事（GitHub Pages 門面）

孩子在 iPhone／iPad Safari 開的入口。音檔在家裡 `F:\KoDoHoN\library`，不上這個庫。

線上入口：<https://weslie4436.github.io/kodohon/>

家裡保險庫（`python -m kodohon vault`，埠 8767）經 Cloudflare 隧道連上來。隧道網址寫進 `config.js` 的 `VAULT_ORIGIN`。鑰匙不進 git。
